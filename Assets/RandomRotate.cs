using UnityEngine;
using System.Collections;

public class RandomRotate : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		transform.Rotate (.1f,.2f,.1f);
	}
}
